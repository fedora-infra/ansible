---
dependencies:
    - { role: linux-system-roles.network, when: "'cloud_aws' not in group_names", tags: ['linux-system-roles.network'] }
    - { role: basessh }
    - { role: chrony }
    - { role: dnf-automatic }
