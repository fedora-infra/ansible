{% macro load_file(filename) %}{% include filename %}{%- endmacro -%}
---
apiVersion: v1
kind: Secret
metadata:
  name: kanban-secret
  labels:
    app: kanban
stringData:
  client-secrets.json: |-
    {{ load_file('client-secrets.json') | indent }}
