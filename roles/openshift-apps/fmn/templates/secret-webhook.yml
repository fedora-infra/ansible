---
apiVersion: v1
kind: Secret
metadata:
  name: fmn-github-webhook
data:
  WebHookSecretKey: "{{ (env == 'production')|ternary(fmn_prod_github_secret, fmn_stg_github_secret)|b64encode }}"
type: Opaque
