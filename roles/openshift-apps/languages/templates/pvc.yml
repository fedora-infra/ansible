apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ app }}-storage"
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
{%    if env == 'staging' %}
      storage: 40Gi
{%    else %}
      storage: 70Gi
{%    endif %}
  storageClassName: ""
