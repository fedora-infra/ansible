apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    monitoring-key: 'middleware'
  name: bodhi-web
spec:
  endpoints:
    - path: /metrics
      port: web
  selector:
    matchLabels:
      app: bodhi