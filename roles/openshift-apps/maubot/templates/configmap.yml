{% macro load_file(filename) %}{% include filename %}{%- endmacro -%}
---
apiVersion: v1
kind: List
metadata: {}
items:
- apiVersion: v1
  kind: ConfigMap
  metadata:
    name: config
    labels:
      app: maubot
  data:
    config.yml: |-
      {{ load_file('config.yml') | indent(6) }}
