apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: planet-http-storage
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Mi
  storageClassName: ocs-storagecluster-cephfs
