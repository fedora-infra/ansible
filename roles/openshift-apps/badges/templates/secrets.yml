---
kind: Secret
apiVersion: v1
metadata:
  name: badges
stringData:
{% if env == 'staging' %}
  oidc-client-secret: {{ badges_stg_oidc_client_secret }}
{% else %}
  oidc-client-secret: {{ badges_prod_oidc_client_secret }}
{% endif %}
