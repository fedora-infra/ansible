---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: badges
    service: web
  namespace: badges
spec:
  ports:
    - name: web
      port: 8080
      targetPort: 8080
  selector:
    deploymentconfig: frontend
