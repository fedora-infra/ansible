apiVersion: v1
kind: Service
metadata: 
  name: "resultsdb-frontend"
  labels:
    app: resultsdb
    service: web
spec:
  selector:
    app: resultsdb
    service: web
  ports:
    - name: web
      port: 5001
      targetPort: 5001
