---
apiVersion: v1
kind: Service
metadata:
  name: noggin-web
  labels:
    app: {{ object_app }}
spec:
  ports:
    - name: web
      port: 8080
      targetPort: 8080
  selector:
    app: {{ object_app }}
    deploymentconfig: noggin
